import {
  View,
  Text,
  Image,
  TouchableOpacity,
  ScrollView,
  TextInput,
  StatusBar,
} from "react-native";
import React from "react";

const Messager = () => {
  return <View style={{ paddingTop: StatusBar.currentHeight }}></View>;
};

export default Messager;
